# Heatmap plugin render tests

These fixtures are the built-in MapLibre Native heatmap render tests with only
the layer type changed from `heatmap` to `org.maplibre.heatmap`. The shared
plugin render-test runner discovers this manifest and registers the plugin
before loading the styles.
